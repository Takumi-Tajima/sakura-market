.row.row-cols-1.row-cols-md-3.row-cols-lg-4
  - @items.each do |item|
    .col.mb-5.text-decoration-none
      = link_to item_path(item), class: 'text-decoration-none' do
        .card.shadow-lg
          = image_tag item.item_img.variant(:thumb)
          .card-body
            %h3.h5.card-title= item.name
            .card-text
              = p_with_tax(item)

