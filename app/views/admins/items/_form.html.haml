= simple_form_for([:admins, item]) do |f|
  = f.file_field :item_img
  = f.input :name
  = f.input :price
  = f.input :description
  = f.input :published
  = f.button :submit, class: 'btn btn-primary'
