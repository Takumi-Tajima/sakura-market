.row.mt-4
  .col-md-6
    = item_img_tag(@item, :display)
  .col-md-6
    %h2.mt-2
      = @item.name
    .mt-2
      = price_with_tax(@item)
    .mt-2
      = @item.description

= link_to '編集', edit_admins_item_path(@item), class: 'btn btn-primary mt-3'

= link_to '上へ', move_higher_admins_item_path(@item)
= @item.position
= link_to '下へ', move_lower_admins_item_path(@item)
